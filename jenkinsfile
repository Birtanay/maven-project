pipeline {
    agent any

    stages{
        stage('Build'){
            steps {
                bat 'C:\\maven\\apache-maven-3.6.0\\bin\\mvn clean package'
                bat "docker build . -t tomcatwebapp:${env.BUILD_ID}"
            }
            

        }
    }
}